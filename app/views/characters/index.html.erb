<% provide(:title, 'My Roster') %>

<h1><%= yield(:title)%></h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Realm</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%= render @characters %>
  </tbody>
</table>

<br>

<%= link_to 'New Character', new_character_path %>
